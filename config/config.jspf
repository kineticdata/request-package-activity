<%-- Set the values of the packages configurable settings. --%>
<%
    // Store the template id of the portal page so that it can be passed to ajax
    // requests.
    bundle.setProperty("templateId", customerRequest.getTemplateId());
    bundle.setProperty("sourceError", "A source error has occurred. Please contact your system administrator.");
%>